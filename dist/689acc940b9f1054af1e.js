import*as THREE from"three";import{PointLightShadow}from"three";const scene=new THREE.Scene,renderer=new THREE.WebGLRenderer;renderer.setSize(window.innerWidth,window.innerHeight),document.body.appendChild(renderer.domElement);const pointLight=new THREE.PointLight(4210752,10);pointLight.position.set(0,20,0);const geometry=new THREE.BoxGeometry(50,5,50),material=new THREE.MeshPhysicalMaterial({}),plane=new THREE.Mesh(geometry,material);plane.position.set(0,0,0),plane.material.color.setHex(16751104);const SphereGeometry=new THREE.BoxGeometry(5,5,5),sphereMaterial=new THREE.MeshPhysicalMaterial({}),sphere=new THREE.Mesh(SphereGeometry,sphereMaterial);sphere.material.color.setColorName("red"),sphere.position.set(0,10,0);const camera=new THREE.PerspectiveCamera(100,window.innerWidth/window.innerHeight,1,1e3);function animate(){requestAnimationFrame(animate),renderer.render(scene,camera)}camera.position.x=sphere.position.x,camera.position.y=sphere.position.y+7,camera.position.z=sphere.position.z+15,camera.lookAt(sphere.position),scene.add(pointLight),scene.add(plane),scene.add(sphere),animate();